version: '3.8'

services:
  app:
    build: .
    ports:
      - "1026:3000"
    environment:
      - DATABASE_URL=mongodb://mongo:27017/mydatabase
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
